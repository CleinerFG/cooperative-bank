export const SERVER_IP = 'localhost';
export const API_BASE_URL = `http://${SERVER_IP}:8080/api`;

/** --- Dev test --- */

/** Time in seconds when making a request to the server API */
export const SIMULATE_WAIT_SERVER = 0;
