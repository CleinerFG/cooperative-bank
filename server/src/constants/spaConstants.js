const path = require('path');

const APP_DIR = path.resolve(__dirname, '../../../client/dist/app');
const PUBLIC_DIR = path.resolve(__dirname, '../../../client/dist/public');

module.exports = { APP_DIR, PUBLIC_DIR };
